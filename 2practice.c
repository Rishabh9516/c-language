#include <stdio.h>

int main()
{
    int radius = 5;
    float pi = 3.14;
    int hieght = 4;
    printf("the area of circle is %f\n", pi * radius * radius);
    printf("the volume of cylinder is %f\n", pi * radius * radius * hieght);
    return 0;
}